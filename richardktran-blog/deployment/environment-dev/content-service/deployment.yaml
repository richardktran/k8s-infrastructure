apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: "content-service"
  name: "content-service"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "content-service"
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: "content-service"
    spec:
      containers:
        # Get image from richardktran/content-service:devevelopment:{version}
        - image: __image__
          imagePullPolicy: "Always"
          name: "content-service"
          env:
            - name: "REACT_APP_ENVIRONMENT"
              value: "PROD"
          ports:
            - containerPort: 80
              protocol: TCP
          terminationMessagePath: /dev/termination-log # this is when the container is terminated, it will write the log to this path
          terminationMessagePolicy: File
      restartPolicy: Always
