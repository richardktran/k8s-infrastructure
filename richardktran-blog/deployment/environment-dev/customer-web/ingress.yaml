apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: app
  namespace: ingress-controller
spec:
  rules:
  - host: richardktran.local
    http:
      paths:
      - path: /
        backend:
          serviceName: customer-web
          servicePort: 80