apiVersion: v1
kind: Service
metadata:
  labels:
    app: "comment-service"
  name: "comment-service"
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: "comment-service"
  sessionAffinity: None
  type: NodePort
