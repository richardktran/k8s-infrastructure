apiVersion: v1
kind: Service
metadata:
  labels:
    app: "comment-service"
  name: "comment-service"
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: "comment-service"
  type: ClusterIP
